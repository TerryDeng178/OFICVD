# P0 优化测试结果报告

**测试时间**: 2025-11-07 04:31:37  
**Run ID**: 20251107_043137  
**配置**: `defaults.smoke.yaml`

---

## ✅ 重大改进

### 1. 确认信号大幅提升
- **之前**: 0 条确认信号
- **现在**: **69,324 条确认信号**（28.3% 确认率）
- **改进**: ✅ consistency 保守底座生效

### 2. 信号类型分布正常
- **buy**: 31,544 条
- **sell**: 31,597 条
- **strong_buy**: 3,010 条
- **strong_sell**: 3,173 条
- **neutral**: 174,493 条

### 3. low_consistency 占比下降
- **之前**: 93.2% (219,715 / 235,845)
- **现在**: 64.9% (158,649 / 244,579)
- **改进**: 下降了 28.3 个百分点

---

## ⚠️ 仍需优化的问题

### 1. Regime 分布仍为 100% quiet
- **问题**: 所有信号仍被判定为 `quiet`
- **原因**: 活动度字段（`trade_rate`, `quote_rate`, `realized_vol_bps`, `volume_usd`）为 `null`
- **根因**: FeaturePipe 读取的是 preview features 数据（已处理），不包含原始 trade/orderbook 数据，无法计算活动度

### 2. 活动度字段未写入
- **问题**: `trade_rate`, `quote_rate`, `realized_vol_bps`, `volume_usd` 字段为 `null`
- **原因**: 
  - Preview features 数据已经是处理过的特征数据
  - 没有原始 trade/orderbook 数据，窗口更新逻辑无法执行
  - 导致活动度计算返回 0，但字段可能没有被正确写入

### 3. low_consistency 占比仍较高
- **当前**: 64.9% (158,649 / 244,579)
- **目标**: <50%
- **建议**: 进一步优化 consistency 保守底座逻辑

---

## 📊 详细统计

### 处理统计
- **processed**: 244,579
- **emitted**: 69,324 (28.3%)
- **suppressed**: 175,255 (71.7%)
- **warmup_blocked**: 16,606 (6.8%)
- **deduplicated**: 0

### 拦截原因分布
| 拦截原因 | 数量 | 占比 |
|---------|------|------|
| `low_consistency` | 158,649 | 64.9% |
| `warmup` | 16,606 | 6.8% |

### 按交易对统计
| 交易对 | 总行数 | confirm | 确认率 |
|--------|--------|--------|--------|
| BTCUSDT | 62,570 | 16,498 | 26.4% |
| ETHUSDT | 63,110 | 15,403 | 24.4% |
| SOLUSDT | 61,900 | 20,990 | 33.9% |
| BNBUSDT | 56,999 | 16,433 | 28.8% |

---

## 🔍 问题分析

### 活动度字段为 null 的原因

1. **数据源问题**:
   - 测试使用的是 `preview/kind=features/*.parquet` 数据
   - 这些数据已经是处理过的特征数据，不包含原始 trade/orderbook
   - FeaturePipe 的窗口更新逻辑需要原始数据才能工作

2. **代码逻辑问题**:
   - FeaturePipe 的 `on_row` 方法期望原始数据格式
   - 当读取 preview features 数据时，数据格式不匹配
   - 导致窗口更新逻辑无法执行，活动度计算返回 0

3. **字段写入问题**:
   - 代码中字段应该被写入，但实际输出中字段为 `null`
   - 可能是计算值为 0 时被序列化为 `null`，或者字段根本没有被写入

---

## 🎯 下一步优化建议

### P0（立即修复）

1. **修复活动度字段写入问题**
   - 检查为什么字段没有被写入到 feature_row
   - 确保即使值为 0 也要写入字段（不要写 null）

2. **处理 preview features 数据源**
   - 如果读取的是 features 数据，应该直接输出，不需要重新计算
   - 或者，从 features 数据中提取已有的活动度信息
   - 或者，需要从 raw 数据计算活动度，而不是从 preview features

3. **优化 consistency 保守底座**
   - 进一步降低 `low_consistency` 占比
   - 考虑更智能的 consistency 估算逻辑

### P1（短期优化）

1. **添加数据源类型检测**
   - 检测输入数据是 raw 还是 features
   - 根据数据类型采用不同的处理逻辑

2. **改进活动度计算**
   - 如果数据源是 features，尝试从已有字段推导活动度
   - 或者，提供活动度的默认估算值

3. **添加调试日志**
   - 记录活动度计算过程
   - 记录窗口更新情况
   - 便于排查问题

---

## 📝 代码修改总结

### 已完成的修改

1. ✅ **FeaturePipe 活动度计算**
   - 添加了 4 个滑窗（trade_ts_window, quote_ts_window, ret_window, vol_window）
   - 添加了活动度计算逻辑
   - 添加了字段到 feature_row

2. ✅ **CoreAlgorithm consistency 保守底座**
   - 添加了 consistency 保守底座逻辑
   - 添加了配置参数

3. ✅ **CoreAlgorithm MarketActivity 优化**
   - 优化了 `_create_market_activity` 方法
   - 优先使用真实字段

4. ✅ **StrategyMode 可观测性**
   - 添加了每 10 秒的心跳快照日志

### 待修复的问题

1. ❌ **活动度字段未写入**
   - 需要检查为什么字段没有被写入
   - 可能需要修复数据源处理逻辑

2. ❌ **Regime 仍为 100% quiet**
   - 需要修复活动度计算问题
   - 可能需要调整 StrategyModeManager 阈值

---

## 🎉 成果总结

尽管仍有问题，但 **consistency 保守底座已经显著改善了结果**：

- ✅ 确认信号从 0 提升到 69,324（28.3% 确认率）
- ✅ low_consistency 占比从 93.2% 下降到 64.9%
- ✅ 信号类型分布正常（buy/sell/strong_buy/strong_sell 均有产出）

**下一步**: 修复活动度字段写入问题，解决 Regime 100% quiet 的问题。

---

**报告生成时间**: 2025-11-07  
**报告版本**: v1.0

